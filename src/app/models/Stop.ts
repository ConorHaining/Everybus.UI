export interface Stop {
    atocCode: string;
    stopId: string;
    name: string;
    identifer?: string;
    direction: string;
    latitude: string;
    longitude: string;
    destinations: string[];
}
